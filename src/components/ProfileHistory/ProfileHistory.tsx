// optional part to complement the login data

import React from "react";

export default function ProfileHistory() {
  return (
    <section>
      <h2>Your History</h2>
      <p>These are your past requests:</p>
      {/* map through isOpen: false -requests for user, show basic info and link to detailspage */}
    </section>
  );
}
